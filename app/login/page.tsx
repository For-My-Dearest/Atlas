import { getSession } from '@/lib/auth'; import { redirect } from 'next/navigation';
export default async function Login(){if(await getSession())redirect('/'); return <main className="login"><div className="login-card"><p className="eyebrow">THE LIVING ATLAS</p><h1>Enter the campaign.</h1><p>Secure access for GMs and players.</p><form action="/api/auth/login" method="post"><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label><button className="primary">Sign in</button></form><small>Seed accounts: gm@living-atlas.local / change-me-now · player@living-atlas.local / change-me-now</small></div></main>}
