import {NextResponse} from 'next/server'; import {db} from '@/lib/db'; import bcrypt from 'bcryptjs';
export async function POST(req:Request){const form=await req.formData(); const email=String(form.get('email')??'').trim().toLowerCase(); const password=String(form.get('password')??''); const u=await db.user.findUnique({where:{email}}); if(!u||!(await bcrypt.compare(password,u.passwordHash))) return NextResponse.redirect(new URL('/login?error=1',req.url)); const s=await db.session.create({data:{userId:u.id,expiresAt:new Date(Date.now()+1000*60*60*24*7)}}); const r=NextResponse.redirect(new URL('/',req.url)); r.cookies.set('atlas_session',s.id,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:60*60*24*7}); return r;}
