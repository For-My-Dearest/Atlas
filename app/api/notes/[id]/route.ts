import { NextResponse } from 'next/server';
import { requireMember } from '@/lib/auth';
import { db } from '@/lib/db';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;try{const note=await db.note.findUnique({where:{id}});if(!note)return NextResponse.json({error:'Note not found.'},{status:404});const {session,membership}=await requireMember(note.campaignId);if(note.userId!==session.userId)return NextResponse.json({error:'FORBIDDEN'},{status:403});const b=await req.json();const linkedCharacterId=b.linkedCharacterId||null,linkedLocationId=b.linkedLocationId||null,folderId=b.folderId||null;if(linkedCharacterId){const c=await db.character.findFirst({where:{id:linkedCharacterId,campaignId:note.campaignId,archived:false}});if(!c)return NextResponse.json({error:'Invalid linked character.'},{status:400});if(membership.role==='PLAYER'&&!(await db.characterDiscovery.findUnique({where:{characterId_userId:{characterId:linkedCharacterId,userId:session.userId}}})))return NextResponse.json({error:'You have not discovered this character.'},{status:403});}if(linkedLocationId&&!(await db.location.findFirst({where:{id:linkedLocationId,campaignId:note.campaignId,archived:false}})))return NextResponse.json({error:'Invalid linked location.'},{status:400});if(folderId&&!(await db.noteFolder.findFirst({where:{id:folderId,campaignId:note.campaignId}})))return NextResponse.json({error:'Invalid note folder.'},{status:400});const visibility=membership.role==='GM'&&b.visibility==='PUBLIC'?'PUBLIC':'PRIVATE'; const body=String(b.body||''); const direction=/[\u0590-\u08FF]/.test(body)?'RTL':'LTR';const updated=await db.note.update({where:{id},data:{title:String(b.title||'').trim()||'Untitled note',body:String(b.body||''),linkedCharacterId,linkedLocationId,folderId,visibility,direction}});return NextResponse.json(updated);}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==='FORBIDDEN'?403:401});}}
export async function DELETE(_req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;try{const note=await db.note.findUnique({where:{id}});if(!note)return NextResponse.json({error:'Note not found.'},{status:404});const {session,membership}=await requireMember(note.campaignId);if(note.userId!==session.userId)return NextResponse.json({error:'FORBIDDEN'},{status:403});await db.note.update({where:{id},data:{archived:true}});return NextResponse.json({ok:true});}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==='FORBIDDEN'?403:401});}}
